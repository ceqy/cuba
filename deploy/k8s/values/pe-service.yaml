# pe-service Helm Values
replicaCount: 2
image:
  repository: cuba-erp/pe-service
  tag: "latest"
fullnameOverride: "pe-service"
service:
  grpcPort: 50061
  metricsPort: 9090

# Database configuration (credentials from Secret)
database:
  name: cuba_sd_pe
  secretName: cuba-postgres-credentials

env:
  RUST_LOG: "info"

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 128Mi
