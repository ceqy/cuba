# df-service Helm Values
replicaCount: 2
image:
  repository: cuba-erp/df-service
  tag: "latest"
fullnameOverride: "df-service"
service:
  grpcPort: 50081
  metricsPort: 9090

# Database configuration (credentials from Secret)
database:
  name: cuba_sc_df
  secretName: cuba-postgres-credentials

env:
  RUST_LOG: "info"
  limits: {cpu: 500m, memory: 512Mi}
  requests: {cpu: 100m, memory: 128Mi}

resources:
  limits: {cpu: 500m, memory: 512Mi}
  requests: {cpu: 100m, memory: 128Mi}
