# so-service Helm Values
replicaCount: 2
image:
  repository: cuba-erp/so-service
  tag: "latest"
fullnameOverride: "so-service"
service:
  grpcPort: 50060
  metricsPort: 9090

# Database configuration (credentials from Secret)
database:
  name: cuba_sd_so
  secretName: cuba-postgres-credentials

env:
  RUST_LOG: "info"

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 128Mi
